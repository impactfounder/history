/**
 * 큐레이션 파이프라인이 쓰는 모델 — 한 곳에서만 정한다.
 *
 * history는 무브먼트와 무관한 독립 프로젝트라 회사의 모델 레지스트리(mvmt-core)를
 * 쓰지 않는다(PRD §8, 2026-09-05). 그래도 ID를 코드 여기저기에 흩지는 않는다 —
 * 바꿀 일이 생기면 이 파일 한 줄만 고친다.
 *
 * 비용·품질을 바꾸는 결정이므로 여기를 고칠 때는 이유를 커밋 메시지에 남긴다.
 */
export const DRAFT_MODEL = "claude-opus-5";
